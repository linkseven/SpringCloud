server:
  port: 80

spring:
  application:
    name: cloud-payment-consumer-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}